{
  "devDependencies": {
    "@types/jest": "^28.1.5",
    "jest": "^28.1.2",
    "ts-jest": "^28.0.5",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "@types/node": "^18.0.3",
    "nodemon": "^2.0.19",
    "ts-node": "^10.8.2"
  },
  "scripts": {
    "watch": "nodemon --config nodemon.json",
    "watch:debug": "nodemon --config nodemon.json --inspect-brk ",
    "test": "jest"
  }
}
